var Browser = require('zombie');
var url = "http://localhost:3000/";
var browser = new Browser();

describe('User clicks space on map', function() {
  it("shows space specific details", function {
    browser.visit(url, function(err) {
      expect()
    })
  })

});
